apiVersion: keycloak.hostzero.com/v1beta1
kind: KeycloakRequiredAction
metadata:
  name: example-configure-otp
  namespace: default
spec:
  realmRef:
    name: example-realm
  alias: CONFIGURE_TOTP
  definition:
    name: "Configure OTP"
    providerId: CONFIGURE_TOTP
    enabled: true
    defaultAction: false
    priority: 10
