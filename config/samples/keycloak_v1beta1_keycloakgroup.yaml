apiVersion: keycloak.hostzero.com/v1beta1
kind: KeycloakGroup
metadata:
  name: example-group
  namespace: default
spec:
  realmRef:
    name: example-realm
  name: administrators
  definition:
    attributes:
      department: ["platform"]
